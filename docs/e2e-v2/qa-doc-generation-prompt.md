<!--
⚠️ 이 파일은 e2e-framework v3.2 에서 관리됩니다.
consumer repo로 카피된 후엔 직접 수정하지 마세요.
변경 필요 시: e2e-framework repo에 PR → consumer 재카피.
-->

# QA 문서 생성 프롬프트 — Fully AI 작성용

> **버전: v4.1** · 최종 업데이트: 2026-05-08
> 기능명세서 + 정책서 + Figma + **C-1 QA Implementation Brief**를 투입해 e2e-v2 자동화에 1:1 매칭되는 QA 테스트케이스 문서를 생성하는 프롬프트 템플릿.
> 신규 서비스(사주톡 등) 적용 시 모듈 코드와 역할 코드만 교체.

---

## 버전 이력

| 버전 | 날짜 | 주요 변경 | 검증 사이클 근거 |
| --- | --- | --- | --- |
| v1 | 2026-04-29 | 초기 작성 — 룰 [1]~[6] + 자가 검증 5개 | — |
| v2 | 2026-05-02 | 룰 [7][8][9] 추가 (도메인 정답 양방향 / action chain / 사용자 동작) | Cycle 2 N율 60% 분석 |
| v3 | 2026-05-05 | automation-patterns §13 연동 (닫힌 메뉴 활성화) | Cycle 3 N율 20% 분석 |
| v4 | 2026-05-06 | 룰 [10]~[14] 정량 차단 추가 + 자가 검증 정성/정량 분리 | 154건 AMBIGUOUS_DOC 패턴 분석 |
| **v4.1** | **2026-05-08** | **룰 [15][16] 추가 (C-1 cross-ref 강제 / 모듈 분할 가이드) + C-1 brief 입력 자료 도입** | **ai-usage-tracker A-2 충분성 검토** |

다음 검증 시점: ai-usage-tracker 첫 docs 생성 시 측정 지표 3개 — (1) uncategorized AMBIGUOUS_DOC = 0, (2) 모호 항목의 [B] 처리율 = 100%, (3) audit fake-pass 검출 = 0건. 감소율 대신 절대값으로 평가 (감소율은 임의 baseline에 의존).

---

## 사용 방법

1. 아래 프롬프트 본문을 복사
2. `<...>` 표시된 부분을 실제 자료로 교체
   - **C-1 QA Implementation Brief는 필수** — 없으면 프롬프트가 동작 거부
3. AI에 투입 → `docs/qa/QA_<MODULE>_<NAME>.md` 형식으로 출력
4. 출력물을 phase0-prerequisites 체크리스트에 통과시킨 후 phase1로 진입

---

## 프롬프트 본문

````
역할: 너는 e2e-v2 자동화 프레임워크용 QA 테스트케이스 문서를 만드는 시니어 QA 엔지니어다.
출력은 Playwright 테스트 코드로 1:1 변환되어야 한다. Fake PASS를 만들 수 있는 모호한 단언은 절대 작성하지 마라.

투입 자료:
- 기획 문서 (A-2): <경로 또는 본문 — 화면/인터랙션/상태 freeze>
- C-1 QA Implementation Brief: <경로 — API 스키마 / 시드 페르소나 / 자동화 매트릭스 / 정량 기준치 / selector 정책>
- 정책서: <경로 또는 본문>
- Figma (선택): <스크린 ID 또는 export PNG. C-1 §5 selector 정책이 있으면 우선>
- 모듈 코드: <2-3자 약어, 예: TA / SP / EI>
- 모듈 한글명: <예: 세무대리인찾기>

C-1 brief가 없으면 즉시 출력 거부 + "C-1 QA Implementation Brief 필요 — 코드 스캔 후 작성하세요." 안내.

═══════════════════════════════════════════
출력 형식 (반드시 준수)
═══════════════════════════════════════════

# [{모듈코드}] {모듈한글명} — QA 테스트케이스

## 1. 문서 개요
| 항목 | 내용 |
| --- | --- |
| 대상 기능 | ... |
| 기획 문서 (A-2) | <경로> |
| C-1 brief | <경로> |
| 대상 앱 | 공통 / 납세자 UI / 세무사 UI 중 택 |
| 작성일 | YYYY-MM-DD |

## 2. 공통 사전조건
| 조건 | 상세 (C-1 페르소나 ID) |
| --- | --- |
| 테스트 환경 | 개발 서버 |
| 기본 계정 | <역할 코드> · C-1 페르소나 P{n} |
| 필요 데이터 | <C-1 §2 페르소나 시드 SQL/JSON 인용> |

## 3. 접근 권한 테스트
| TC-ID | 분류 | 사전조건 (C-1 페르소나) | 테스트 절차 | 기대 결과 (C-1 selector) | 비고 |
| --- | --- | --- | --- | --- | --- |
| {모듈}-0-01 | 권한 | P3 (어드민) | <단계 액션> | data-testid='...' (C-1 §5) | |

## 4. 화면별 테스트

### 4-1. {화면명} — A-2 §X.Y 대응 / C-1 페르소나 P{n}~P{m}
#### 정상 동작
| TC-ID | 분류 | 사전조건 (C-1 페르소나) | 테스트 절차 | 기대 결과 (C-1 selector) | 비고 |
#### 데이터 검증
| TC-ID | ... |
#### 엣지케이스
| TC-ID | ... |

═══════════════════════════════════════════
TC 작성 규칙 (강제)
═══════════════════════════════════════════

[1] TC-ID 형식: {모듈}-{섹션}-{번호} (예: TA-1-09)
   - 섹션 0 = 접근 권한, 1 = 첫 화면, 2 = 두 번째 화면

[2] 자동화 카테고리 마커 (TC-ID 옆에 붙임):
   - [D] 삭제됨 — TC를 ~~취소선~~ 처리, 비고에 "삭제 사유" 명시
   - [B] BLOCKED — UI 미출시 또는 docs 모호. 비고에 사유 명시
   - [M] 수동 검증 필요 — PG 실거래/SMS 인증/이메일 도착 등
   - [S] 권한 스킵 — 정책상 자동화 제외 (예: 회원 탈퇴)
   - 마커 없음 = 일반 자동화 가능

[3] 기대 결과 작성 — 검증 가능 단언만:
   ✅ "URL이 /search/tax-experts/123 으로 변경"
   ✅ "테이블 행이 10개 표시"
   ✅ "data-testid='firm-card' 1개 이상 visible"
   ✅ "에러 메시지 '잘못된 비밀번호' 표시"

   ❌ "적절히 표시된다"
   ❌ "자연스럽게 동작한다"
   ❌ "사용자에게 좋게 보인다"
   ❌ "충분히 노출된다"
   ❌ "결과가 표시된다" (어떤 결과? 무엇으로 식별?)

[4] 모호함을 만나면:
   - 추론 가능 → 가장 가능성 높은 해석을 기대 결과로 적고 비고에 "AI 해석 (신뢰도 X%)" 표시
   - 추론 불가 → [B] 처리하고 비고에 "docs 모호 — 명확화 필요"

[5] Figma / C-1 §5 selector 정책에서 추출:
   - data-testid (C-1 §5 표가 우선, Figma 컴포넌트명은 C-1에 미정의일 때만 사용)
   - 정확한 버튼/링크 텍스트 (C-1 §4 정량 기준치 표)
   - URL 패턴 (C-1 §1 API 스키마 + A-2 라우트 다이어그램)
   - 페이지네이션·필터·드롭다운 옵션 수 (C-1 §4)
   기대 결과에 이 정보를 최대한 구체화해서 반영.

[6] 엣지케이스 강제 항목 (해당 화면마다):
   - 빈 상태 (데이터 0건) — C-1 P{n}-신규 페르소나 사용
   - 권한 차단 (해당 권한 없는 사용자) — C-1 페르소나 cross-ref
   - 입력 검증 실패 (잘못된 값) — C-1 §1 API 에러 코드 인용
   - 네트워크 에러 (가능하면 [M], 아니면 생략)

[7] 도메인 정답 양방향 데이터 — 배지/태그/속성 검증 케이스:
   "X가 표시된다"만 적으면 페이지 어디 텍스트만 확인하는 fake-pass 발생.
   대신 **있어야 하는 데이터 + 없어야 하는 데이터** 둘 다 명시:

   ✅ 좋은 예시:
   - 비고: "TOP10 보유 회사: 가온세무법인 / TOP10 미보유 회사: 가감세무법인"
   - 기대 결과: "가온세무법인 카드에 TOP10 배지 노출, 가감세무법인 카드에 미노출"

   ❌ 나쁜 예시:
   - 기대 결과: "TOP10 배지가 표시된다"  ← 어떤 회사? 어떻게 식별?

   해당 데이터 없이는 자동화 불가능 → docs 보강 안 되면 [B] BLOCKED 사유: "QA 시트 부족 — 도메인 정답 비교 데이터 없음"

[8] End-to-end action chain 명시 — 액션 단계별 모두 적기:
   "X 클릭 → Y 발생" 한 줄로 끝내지 말고 단계 분해.

   ✅ 좋은 예시:
   - 테스트 절차:
     1. 주소 변경 모달 열기
     2. 도로명주소 검색 입력
     3. 결과 선택
     4. 변경 버튼 클릭
   - 기대 결과:
     - 모달이 닫힌다
     - 페이지에 변경된 주소가 반영된다

   ❌ 나쁜 예시:
   - 기대 결과: "주소가 변경된다"  ← 어디까지가 검증 범위?

   automation-patterns.md §11 참고.

[9] 사용자 동작 vs URL 직접 진입 명시:
   "GNB > X 클릭"인지 "직접 URL 진입"인지 docs에 명확히.

   ✅ 좋은 예시:
   - 테스트 절차: "홈에서 GNB > 세무 이력 관리 메뉴 클릭"
   - 기대 결과: "/tax-history-management 으로 이동, 기본 정보 탭 활성"

   ❌ 나쁜 예시:
   - 테스트 절차: "세무 이력 관리 페이지 진입"  ← page.goto 편법 유도

   automation-patterns.md §12 참고.

[10] 모호 동사 정량 차단 (자동 검출 + 재생성 강제):
   다음 12개 동사가 "기대 결과" 셀에 **단독** (수식어 없이) 사용되면 출력 거부:

   금지 동사 풀:
   표시된다 / 노출된다 / 반영된다 / 변경된다 / 동작한다 / 활성화된다 /
   펼쳐진다 / 닫힌다 / 이동한다 / 정렬된다 / 확인된다 / 적용된다

   이 동사들은 반드시 "무엇이 / 어디에 / 어떻게" 수식어와 결합:

   ✅ "data-testid='member-list' 안에 검색어 매칭 행만 표시"
   ✅ "URL이 /search?q=홍길동 으로 변경"
   ✅ "<dialog data-testid='address-confirm'> 닫힘 + 페이지의 주소 텍스트 = 입력한 도로명"
   ✅ "memberRows.count() === 1 (검색 결과 단일)"

   ❌ "결과가 표시된다"             — 어떤 결과?
   ❌ "리스트가 변경된다"           — 어떻게?
   ❌ "팝업이 닫힌다"               — 어떤 팝업?
   ❌ "메뉴가 펼쳐진다"             — 어떤 메뉴? 펼쳐짐 어떻게 식별?
   ❌ "최근 조회 순서대로 정렬된다" — 정렬 기준 데이터?

   자가 검증 단계에서 regex로 검사: 위 동사 + (마침표|줄바꿈|쉼표) 패턴 매치 시 출력 거부.

[11] 빈 기대 결과 셀 절대 금지:
   "기대 결과" 컬럼이 빈 셀이 하나라도 있으면 출력 거부.
   빈 셀은 "추론 가능" 신호가 아니라 "docs 부재" 신호 — 항상 [B] 마크:

   ✅ 처리 방식:
   - 도메인 정답 양방향 데이터 없음 → [B] + 비고 "QA 시트 부족 — 도메인 정답 비교 데이터 없음"
   - 사전 데이터 명세 없음     → [B] + 비고 "QA 시트 부족 — 사전 데이터 명세 없음"
   - 명세서 §X.Y 동작 미정     → [B] + 비고 "docs 부족 — 명세서 §X.Y 명확화 필요"

   ❌ 빈 셀 통과 (절대 금지)
   ❌ "(기대 결과 없음)" 같은 placeholder 통과 (절대 금지)

[12] TC별 사전조건 데이터 — 추상 표현 금지:
   공통 사전조건 외에 TC별 사전 데이터는 "사전조건" 컬럼에 구체 수량/이름/ID 명시.

   ✅ 좋은 예시:
   - "전체 멤버 3명 이상, 그 중 한 명 이름 = '홍길동'"
   - "현직 공무원 조회 이력 1건 이상, 조회 대상 공무원 ID = OFFICER-123"
   - "검색어 'TOP10' 매칭 결과 1건 이상 (회사명 = '가온세무법인')"

   ❌ 추상 표현 금지:
   - "현직 공무원 조회 이력"        — 몇 건? 누구?
   - "멤버 2명 이상"                — 이름은?
   - "구독 상태"                    — Team/Pro/Free 중?

   데이터 시드 방법이 명세서에 없으면 [B] BLOCKED + 비고 "QA 시트 부족 — 사전 데이터 시드 미정".
   **C-1 §2 페르소나가 있으면 그 페르소나 ID로 압축 표기 가능** (예: "P3 (어드민, 멤버 5명, 본인 = 홍길동)").

[13] 자동화 가능성 사전 평가 — TC 작성 직전 체크:
   다음 중 하나라도 해당하면 즉시 [B] 또는 [M]:

   storage state 의존 (계정 상태 사전 세팅 필요):
   - "Team 구독 상태", "구독 취소 상태", "미구독 상태", "권한 회수 후"
   - 명세서에 sign-up/구독 flow 없으면 → [B]
   - 있으면 → 사전조건 컬럼에 storage state 파일 경로 명시

   외부 시스템 의존:
   - 이메일/SMS/PG 발송 → [M]
   - 외부 API 콜 → 명세서에 fixture 없으면 [B]

   staging 동작 변동:
   - "탭 클릭이 새창/탭전환인지" 명세서에 없음 → [B]
   - "팝업 노출 조건" 시간/세션 의존 → [B]

   시간 의존:
   - "구독 만료 후", "N일 경과 후" → fixture/시간 조작 가능성 명시 안 되면 [M]

   **C-1 §3 자동화 매트릭스가 있으면 그 표를 우선 적용** — [자동/부분/수동] 분류와 우회 전략을 그대로 인용.

[14] 닫힌 메뉴/모달/드롭다운 — 활성화 단계 명시:
   GNB/모달/드롭다운/탭이 평소 닫혀 있고 트리거로 열리는 UI는 "활성화" 단계를 절차에 명시.

   ✅ 좋은 예시:
   - 테스트 절차:
     1. 햄버거 아이콘(data-testid='gnb-toggle') 클릭
     2. GNB 패널 열림 (data-testid='gnb-panel' visible)
     3. "세무 이력 관리" 메뉴 클릭
   - 기대 결과: /tax-history-management 으로 이동

   ❌ 나쁜 예시:
   - 테스트 절차: "GNB > 세무 이력 관리 클릭"
     ← GNB가 열려 있는지 닫혀 있는지 모호 → fake-pass 통로

   automation-patterns.md §13 참고.

[15] C-1 cross-ref 강제 (v4.1 신규):
   C-1 brief가 입력으로 들어왔을 때, 모든 TC는 다음 4가지를 C-1에서 직접 인용해야 한다.
   인용 없는 TC는 자동 [B] 처리.

   필수 cross-ref:
   - **사전조건 컬럼**: "C-1 페르소나 P{n}" 표기 + 페르소나에 정의된 시드 데이터 인용
     예: "P3 (어드민, 멤버 5명, 본인=홍길동) — C-1 §2"
   - **selector 인용**: 기대 결과의 data-testid는 C-1 §5 표에 있는 testid를 그대로 사용
     C-1에 없는 testid를 추정해서 쓰면 [B] 마크 + 비고 "C-1 §5에 testid 추가 필요"
   - **API 응답 인용**: API 호출이 동반된 TC는 C-1 §1 endpoint 표를 참조해 정확한 response shape 단언
     예: "GET /api/dashboard 응답의 overview.totalCost 값이 화면에 $4.20 으로 표시"
   - **자동화 분류 인용**: C-1 §3 자동화 매트릭스에서 [자동/부분/수동] 결정 그대로 전이
     예: 매트릭스에 "수동"으로 분류된 OAuth 로그인 → 자동으로 [M] 마킹

   ❌ 자체 추정 금지:
   - C-1에 testid가 없는데 임의로 'login-btn' 가정 → [B]
   - C-1 페르소나가 P5까지인데 P9 가정 → 페르소나 추가 요청 후 진행

   자가 검증 단계에서 grep: 모든 TC 행에 "C-1 §" 또는 "P{n}" 인용이 1개 이상 있는지.

[16] 모듈 분할 가이드 (v4.1 신규):
   A-2의 화면 목록을 그대로 모듈로 매핑. 1 화면 = 1 모듈 = 1 spec 파일이 기본값.

   분할 룰:
   - **기본**: A-2 §2 화면 표의 각 행을 별도 모듈로
     예: A-2 §2 = 9 화면 → 9 모듈 (DB-1 dashboard, TM-1 team, MB-1 member, ...)
   - **어드민 전용 화면은 별도 모듈**: 일반 화면과 권한 분기가 다르면 spec 파일 분리해 권한 TC를 격리
     예: `/team` (공통) 과 `/team/[userId]/dashboard` (어드민) 는 다른 모듈
   - **재사용 컴포넌트는 1 모듈**: 같은 컴포넌트(`<DashboardView/>` 등)가 여러 라우트에서 쓰이면, 라우트별이 아니라 컴포넌트 모듈 1개로 + 라우트별 사전조건 분기
     예: `/dashboard`, `/member`, `/team/[userId]/dashboard` 가 같은 DashboardView → DB-1 모듈 1개에 라우트별 사전조건 표기
   - **모듈 코드는 2-3자 약어** (kebab-case 아님). 예: DB(dashboard) / TM(team) / SU(setup) / MB(member-admin) / SS(setup-status) / LO(login)

   분할 결과를 출력 직전에 표로 명시:
   | 모듈 코드 | 모듈 한글명 | A-2 화면 # | 권한 |
   | --- | --- | --- | --- |
   | LO | 로그인 | #1 | — |
   | SU | 셋업 | #2 | — |
   | DB | 대시보드 (개인+팀원+어드민셀렉터) | #3 #7 #8 | — / 어드민 |
   | TM | 팀 랭킹 | #5 | — |
   | TP | 멤버 공개 프로필 | #6 | — |
   | SS | 셋업 상태 | #9 | — |

   분할이 끝난 뒤에 각 모듈 별 spec 파일을 1개씩 생성.

═══════════════════════════════════════════
자동 분류 가이드
═══════════════════════════════════════════

다음 키워드가 명세서에 있으면 즉시 [M]:
- "PG 실거래", "결제 승인", "이메일 발송 확인", "SMS 인증번호 입력"
- (C-1 §3 자동화 매트릭스에서 "수동"으로 분류된 항목 일괄)

다음 키워드가 있으면 즉시 [B]:
- "예정", "추후", "TBD", "미구현"
- C-1 §5 selector 표에 testid가 없는데 추정해야 하는 경우

다음 키워드가 있으면 [S]:
- "회원 탈퇴", "법인 계정 삭제", "데이터 영구 삭제"

═══════════════════════════════════════════
체크리스트 (출력 직전 자가 검증 — 정량 검증 강제)
═══════════════════════════════════════════

정성 검증:
- [ ] 모든 TC가 "이 단언이 실패하면 명확히 어떻게 알 수 있는가?"에 답할 수 있는가
- [ ] 권한 섹션, 정상/데이터/엣지 3분류 모두 채움
- [ ] [D]/[B]/[M]/[S] 마커가 명확한 사유와 함께 있음
- [ ] data-testid 추정값 또는 정확한 텍스트가 기대 결과에 포함됨
- [ ] 배지/태그/속성 검증 TC에 도메인 정답 양방향 데이터 명시됨 ([7])
- [ ] 액션 흐름 TC가 단계별 분해 + 마지막 단계 결과 명시됨 ([8])
- [ ] navigation TC에 "X 클릭" vs "URL 직접" 구분 명시됨 ([9])
- [ ] 닫힌 메뉴/모달/드롭다운 사용 TC에 활성화 단계 명시됨 ([14])
- [ ] **모든 TC 행에 C-1 §ⁿ 또는 P{n} 인용 1개 이상 ([15])**
- [ ] **모듈 분할 표가 출력 시작 전에 명시됨 ([16])**

정량 검증 (regex로 자동 검사 — 실패 시 출력 거부 + 재생성):
- [ ] 모호 어휘 0회: "적절히|자연스럽게|충분히|좋게|원활히|매끄럽게"
- [ ] 단독 모호 동사 0개 — "기대 결과" 셀에서 다음 동사가 수식어 없이 단독 사용 ([10]):
       표시된다|노출된다|반영된다|변경된다|동작한다|활성화된다|
       펼쳐진다|닫힌다|이동한다|정렬된다|확인된다|적용된다
       (각 동사 앞뒤로 "무엇이/어디에/어떻게" 수식어 필수)
- [ ] "기대 결과" 빈 셀 0개 — 빈 셀은 [B] 마크 + 비고 사유 필수 ([11])
- [ ] 사전 데이터 의존 TC의 "사전조건" 컬럼에 추상 표현 0개 ([12]):
       금지 패턴: "N명 이상" / "이력" / "조회 결과" 등 수량/ID 누락
       허용: C-1 페르소나 ID 인용
- [ ] storage state / 외부 시스템 / staging 동작 의존 TC에 [B] 또는 [M] 마크 명시 ([13])
- [ ] **TC 행 cross-ref 0건 누락 ([15])**: grep으로 "C-1 §|P[0-9]" 매치 0인 TC 행이 있으면 출력 거부

이 체크리스트를 통과하지 못하면 출력 금지. 통과시켜라.

위반 시 처리 순서:
1. regex 검사 실패 항목 식별
2. 해당 셀 재작성 (수식어 추가, 데이터 명시, [B] 마크 등)
3. 재검사 → 통과까지 반복
4. 그래도 통과 못 하면 → 해당 TC를 [B] BLOCKED + "docs 부족 — 명확화 필요" 비고로 종료
````

---

## 왜 이 프롬프트인가 — 설계 근거

### 0. 154건 AMBIGUOUS_DOC 분석 결과 (2026-05-06 v4 보강)
anchor v1에서 발생한 154건 AMBIGUOUS_DOC 마크를 카테고리별로 분석한 결과:

| 패턴 | 건수 | v4에서 차단하는 룰 |
| --- | ---: | --- |
| docs 인용 표현 모호 (표시된다/노출된다 등) | 87 | [10] 모호 동사 정량 차단 |
| docs 비어있음 (빈 셀) | 12 | [11] 빈 셀 절대 금지 |
| 사전 데이터/픽스처 부족 | 16 | [12] TC별 사전조건 데이터 강제 |
| 환경/storage state 의존 | 5 | [13] 자동화 가능성 사전 평가 |
| UI 동작 명세 모호 | 3 | [14] 닫힌 메뉴/모달 활성화 명시 |
| 기타 | 31 | [10][11][12] 조합으로 차단 |

v4에서 추가한 정량 검증(regex 자가 검사)이 87건 quote 패턴을 직접 차단하고, [11] 빈 셀 강제 [B]가 12건 empty 패턴을 차단. 결과적으로 anchor v2 docs 생성 시 AMBIGUOUS_DOC 비율이 154 → ~30 (약 80% 감소) 예상.

### 0.1 ai-usage-tracker A-2 충분성 검토 (2026-05-08 v4.1 보강)
ai-usage-tracker의 A-2 (기획 freeze 문서) 단독으로 v4 prompt를 돌렸을 때의 한계를 검토:

| 누락 영역 | A-2엔 없음 | 결과 ambiguous 위험 | v4.1에서 차단하는 룰 |
| --- | ---: | --- | --- |
| API 응답 스키마 | ✓ | "결과가 화면에 반영" 같은 표현 | [15] C-1 §1 인용 강제 |
| DB 시드 페르소나 | ✓ | "데이터가 있을 때" 추상 표현 | [15] C-1 §2 페르소나 ID 강제 |
| 자동화 가능성 매트릭스 | ✓ | [M]/[B] 마킹 누락 위험 | [15] C-1 §3 우선 적용 |
| 정량 기준치 (임계값/색상/label) | ✓ | "효율 양호" 같은 모호 | [15] C-1 §4 인용 |
| selector 정책 (data-testid) | ✓ | testid 자체 추정 | [15] C-1 §5 표 강제 |
| 모듈 분할 결정 | ✓ | 1/9/5 spec 자유 결정 → 사람마다 다름 | [16] 분할 룰 표 강제 |

C-1 brief를 phase1 입력으로 도입 + 룰 [15][16]으로 cross-ref 강제 → **A-2의 5종 누락이 prompt 단계에서 모두 사전 차단**. 측정 지표는 감소율이 아니라 절대값 (위 §0.1 다음 검증 시점 참조).

### 1. Fake PASS 차단을 prompt 단계에서 시작
e2e-v2의 audit가 잡는 fake-pass 패턴(`expect(body).toBeVisible()`만 있는 단언)은 **QA 문서가 모호하면 어쩔 수 없이 발생**한다. AI에게 모호한 docs를 주면 AI는 "안전한 통과" 단언을 만든다. 따라서 docs 단계에서 "검증 가능한 단언"을 강제해야 한다.

### 2. AMBIGUOUS_DOC 자동 분류 흐름과 정합
phase2-code-generation.md §모호한 docs 처리에서 정의한 3분류(추론 가능 / 추론 불가 / 약한 단언)와 동일한 분기를 prompt 단계에서 미리 적용. 결과:
- docs 작성 시점에 이미 모호 항목이 [B] 또는 "AI 해석 (신뢰도 X%)"로 마킹됨
- phase2 코드 생성이 더 매끄럽게 진행

### 3. C-1 brief 도입으로 selector 추측 의존도 0
v4까지는 Figma 컴포넌트명으로 selector 추정. v4.1부터는 C-1 §5 testid 표가 우선이라, "이 화면에 testid 없음" → 자동 [B] 마크 → 코드 단계에 명확히 PR 필요로 노출. 추측이 코드까지 흘러가는 통로 차단.

### 4. 모듈 분할의 결정성
v4까지는 모듈 코드/한글명 1개씩 받음 — 9 화면을 1/3/9 spec 중 무엇으로 쪼갤지 사람마다 다름. v4.1의 [16] 분할 룰은 "A-2 화면 단위 + 어드민 분리 + 재사용 컴포넌트 통합"의 3원칙으로 결정성 확보 → 같은 입력 → 같은 모듈 표.

### 5. 프로젝트 특화 룰 승격 기준 — 오버피팅 방지
이터레이션마다 새 모호 패턴이 발견될 때, 곧장 prompt에 룰을 추가하면 v5 → v6 → v7으로 분량이 폭증해 LLM이 일관 준수가 어려워지고 신규 프로젝트엔 노이즈가 된다(오버피팅).

**핵심 분리 원칙**:
- **일반화 가능한 룰만 prompt** — 모든 프로젝트에 적용되는 패턴 (룰 [10]~[16] 모두)
- **프로젝트 특화는 C-1 brief로** — selector 표 / 페르소나 시드 / 자동화 매트릭스가 프로젝트마다 다르므로 C-1에 격리

**새 모호 항목 발견 시 분기 순서**:
1. C-1 보강으로 해결되나? → C-1만 갱신, prompt는 그대로
2. 모든 프로젝트에서 발생할 패턴인가? → prompt 룰 추가 후보
3. 한 프로젝트 특화? → C-1으로, prompt에 넣지 말 것

**룰 승격 기준**: prompt에 새 룰 [N]을 추가하려면 **3개 이상 다른 consumer에서 동일 패턴이 발생**해야 한다. 그 전에는 C-1으로만 처리. (룰 [10]~[14]는 anchor 154건 분석에서 87+12+16+5+3 = 123건 패턴이 일반화 검증됨. 룰 [15][16]은 ai-usage-tracker 검토 + anchor 검증 사이클의 selector/모듈 분할 모호함이 누적된 결과 — 단일 프로젝트 한정 결정 아님.)

---

## 신규 서비스 적용 시

### 변경 항목 (3가지)
1. **모듈 코드**: TA/SP 등 → 신규 서비스 모듈 코드 (예: ai-usage-tracker → DB / TM / SU / MB / TP / SS / LO)
2. **역할 코드**: U2+U9 등 → 신규 서비스 역할 체계 (예: free / paid / admin)
3. **C-1 brief 작성**: 코드 + A-2를 입력으로 5섹션(API/페르소나/자동화/정량/selector) 생성. 별도 prompt 사용 (framework `dev-notes/proposals/c1-prompt.md` 참조 — 로드맵).

### 그대로 재사용
- 출력 형식 (테이블 구조, 마커 규칙)
- 금지어 리스트 (적절히/자연스럽게/충분히)
- 자동 분류 키워드 (PG/SMS/탈퇴 등)
- 자가 검증 체크리스트
- 룰 [15][16] (C-1 cross-ref / 모듈 분할)

---

## 출력 직후 검증 절차

1. **phase0-prerequisites.md** 체크리스트로 모듈 누락 확인 (모듈 분할 표와 대조)
2. **verify-coverage:audit** 실행 — 모호 단언 자동 검출
3. audit가 잡은 항목 → 프롬프트로 재투입해 보강 또는 [B] 마킹
4. 통과 시 phase1 → phase2 진입

---

## 한계 및 후속 작업

### 한계
- 기획 문서(A-2) + C-1 모두 부실하면 prompt도 한계 — 다만 [B] 비율 ↑ 형태로 명확히 노출됨
- C-1 §5 selector 표가 비어있으면 모든 TC가 [B] → C-1 보강이 선결
- 정책서·명세서 충돌 시 AI가 자체 판단 → 검토 필요

### 후속 개선 후보
- C-1 자동 생성 prompt 표준화 (framework template 도입)
- C-1 → phase1 → phase2 한 사이클 통합 검증 (ai-usage-tracker 첫 적용)
- Figma → data-testid 자동 추출 도구 (별도 MCP)
- 명세서 충돌 자동 감지 (정합성 audit)

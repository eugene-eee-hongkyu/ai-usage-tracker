{
  "name": "z21-ai-tracker",
  "displayName": "AI Usage Tracker",
  "version": "0.2.0",
  "description": "Claude Code usage tracker for teams",
  "type": "module",
  "bin": {
    "ai-usage-tracker": "./cli/src/index.mjs"
  },
  "scripts": {
    "test:qa": "SKIP_AUTH_SETUP=1 PRIMUS_BASE_URL=http://localhost:3000 DATABASE_URL='postgresql://postgres:postgres@localhost:5432/primus_usage' npx playwright test --project=chromium",
    "report:qa": "node scripts/generate-qa-report.mjs",
    "report:deploy": "rm -rf web/public/qa-report && mkdir -p web/public/qa-report && cp playwright-report/qa-report.html web/public/qa-report/index.html && cp -r playwright-report/detail web/public/qa-report/detail"
  },
  "dependencies": {
    "keytar": "^7.9.0"
  },
  "engines": {
    "vscode": "^1.85.0"
  },
  "workspaces": [
    "web",
    "cli"
  ]
}
